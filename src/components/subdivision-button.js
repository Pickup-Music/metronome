import Button from './button'

class SubDivisionButton extends Button {
  constructor(options) {
    super(options)
  }

  onClick(e) {
    // this.metronome.play()
  }
}

export default SubDivisionButton